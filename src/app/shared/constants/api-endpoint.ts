enum API_ENDPOINT {
  CUSTOMERS = 'customers',
  PRODUCTS = 'products',
  PRODUCT_SEARCH = 'product-projections/search',
  CARTS = 'carts',
  CATEGORIES = 'categories',
}

export default API_ENDPOINT;
